# Copy-ready instruction for the repository's coding agent

Work on Sakushey/gfx1030-dlss-nr-research. Start by comparing the current checkout to audited commit `dcc39b815f9d8f0ccd7b77ab1e5393f32a2645c8`; revalidate changed findings before editing. Read `GTA_V_ENHANCED_DEVELOPER_HANDOVER.md` and the project's contribution, provenance and hardware-testing rules.

Our target is one verifiable GTA V Enhanced frame on RX 6900 XT / gfx1030, produced by a complete, correct selected neural job and applied to its corresponding frozen input frame. Do not claim playable performance, perceptual improvement, gfx1032 support or full-network correctness from a passing isolated kernel.

Implement the following in gated increments. For each increment, provide the changed source, exact reproduction command, test results, input/output artifact identities and outstanding limitations. Keep generated proprietary payloads and model assets local. Preserve existing independent validation and publication checks.

1. Fix the published smoke launcher's source path and provide build-only prerequisite validation. Add a finite-aware numerical comparator with negative controls to both smoke and benchmark paths. Describe the existing dense test accurately and add a distinct native-fragment test. Make numerical emulation reject value stubs. Replace hardcoded readiness with per-profile, artifact-backed stage results.

2. Correct the Clang bundle parser: the u64 at offset 24 is an entry count, not format version. Parse exactly that count within a validated real byte span; do not require a zero terminator. Keep structural parsing separate from exact supported-input manifests. Test counted tables, truncation, overflow, duplicates, nonzero post-table bytes and wrong hashes. The supplied installer has nine entries; do not fix compatibility merely by replacing five with nine or weakening the identity gate.

3. Provide a documented build graph from user-supplied source payload through extraction, metadata, translation, linking, validation, bundling and generated bridge header. Make all legacy/private inputs explicit. Choose one source profile and record source ELF, bundle, model, runtime and output identities. Inspect the selected host's actual HIP imports/ABI; do not assume the historical bridge export set covers a different binary.

4. Adopt the supplied local final-head experiment as an isolated regression case. It passed controlled 1- and 2-workgroup tests, including 32,768 exact output bytes on gfx1030. Reproduce that result before generalizing. Preserve its numerical reference and negative controls. Its fixed descriptor/register allocation, standalone table initialization and saved metadata dependencies are not a production translation strategy. Reproduce globals and initialization through the actual registration path, particularly `g_e4m3_lut`; the public bridge already forwards variable registration and symbol copies.

5. Instrument the chosen GTA/FFX host integration to freeze one authentic input packet and its dispatch graph. Capture dimensions, formats, strides, jitter/exposure/history, constants, full symbols, args, relocatable allocation IDs, launch dimensions, stream dependencies and output composition contract. Do not substitute raw process pointers or fabricated intermediate tensors. If host source or a required contract is unavailable, name the exact blocker and continue independent work.

6. Build a per-symbol qualification ledger for that actual graph, including helper functions and globals. Translate and test every required dependency. Preserve register/control semantics, descriptors, hidden arguments, scratch/LDS, relocations and calls. Treat the reported J3/SWIN watchdog problem as unresolved. Design a bounded checkpoint experiment within the known interval; do not invent its root cause or assume a final-head fix applies. Respect cooperative-wave participation. Stop after timeout/device loss; do not alter TDR or blindly retry.

7. Implement a standalone, one-shot complete-job replay from the frozen packet. Validate meaningful intermediate tensors and final output against a declared independent reference, with justified tolerances, output coverage, guards and input-dependency checks. Run the same frozen job through the real bridge registration path after standalone success. Matching counters or synchronization success alone are insufficient.

8. Add a diagnostic transport/composition mode. Prefer a slow CPU-staged D3D12 readback -> HIP -> D3D12 upload route if shared-resource support blocks the first frame. Respect copy footprints, resource states, fence completion and lifetime. First prove transport with clearly labeled diagnostic patterns; then transfer the verified neural output. Keep Present responsive and waits bounded. Derive residual/color/exposure math from the actual host contract, not assumptions.

9. Capture and preserve one baseline frame and its matching composition inputs; run exactly one qualified job; compose and present that frozen result in GTA; save the raw tensor, pre-UI composed image and displayed capture with matching job/frame ID. Report success only with input/binary identities, dispatch evidence, numerical checks and an identifiable presented frame. Label image-quality assessment separately from execution correctness.

Your first deliverable is the host-only foundation patch and an explicit dependency/profile report, not a claimed working game DLL. All subsequent gates require their own evidence. Propose new file paths and commands clearly as new interfaces; do not describe unimplemented tools as existing. When a result fails, identify the first failing boundary and propose the smallest experiment that can distinguish the supported hypotheses.
